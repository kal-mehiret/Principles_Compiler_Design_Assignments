Detect null dereferencing statically when possible.
You will implement a semantic pass that detects dereferences of variables proven to be null at compile time. They must propagate nullness information through a simplified dataflow analysis.

